# FilmaxWeb 規格需求書

| | |
|---|---|
| 版本 | v6.14 |
| 日期 | 2026-09-04 |
| 決策 | 26 項已確認 |
| 結構 | 這一頁是索引，內容在 `規格/` 一章一檔（v5.9 從 94 KB 單檔拆開） |

**已實作**：**P 瀏覽體驗改善（分頁跳頁／相片瀑布流／開關動畫）** · B 管理後台（六個分區都在）· H 相片檢視 · I PDF 文件庫（最小版）·
J 的修正包（G-6、兩個 falsy-0、NVENC 參數分家、`.env` 畫質值）· Cloudflare／區網 TLS 的運維設定 · **K 字幕預抽（A 案）** · **M 參數頁改版** · **L 目錄權限** · **N 刮削修正包** · **J 的 ffmpeg 升級與 VMAF 量測（v4／v5）** · **J 的分段邊界表 ＋ 本機直讀的路徑對應（第 −1 層／第 0 層第 4 項）** · **J 的量測結論套用（`.env` ＋ `media.py` 的碼率導向）** · **J 的兩階 HLS（已在實機開起來）** · **A 資料層（A-1～A-4）**

**進行中**：（無）

**待做**：C／D／E／F 體驗修正 · I 的 PDF 封面與閱讀進度 · J 的三項量測（libplacebo／spatial-AQ／CRF 複驗） · **O 掃描並行化（需求書已寫，待確認後實作）**

> 排版較好的可讀版本在 Claude Artifact。這一份是進版控的定稿。

---

## 怎麼讀這一份

這一頁是**索引**：只放章節地圖、分期計畫與決策表。內容在 `規格/` 底下一章一個檔案。

- **想知道現在該做什麼** → 下面的〈分期計畫〉。
- **想知道某件事為什麼這樣決定** → 下面的〈已確認的決策〉，那張表是全書的結論摘要。
- **想看某個題目的完整推導與證據** → 章節地圖裡點進去。
- **章別代號在全書通用**（`A-4` 的 `purge()`、`G-6`、`J 的第 −1 層`…）。
  跨章引用一律用代號，不用頁碼也不用檔名 —— 檔案搬家不會讓引用失效。
- **要全文搜尋**：`rg "G-6" docs/`（分成多檔之後，搜尋範圍是 `docs/` 而不是單一檔案）。

## 章節地圖

| 章 | 檔案 | 這一章講什麼 | 狀態 |
|---|---|---|---|
| **0** | [0-修正紀錄.md](規格/0-修正紀錄.md) | v1 的風險評估哪幾條錯了、為什麼 | 背景 |
| **A** | [A-資料層.md](規格/A-資料層.md) | SQLite／MSSQL 雙後端、時間欄位正規化、批次寫入、單一刪除進入點 `purge()` | 待做 |
| **B** | [B-管理後台.md](規格/B-管理後台.md) | 後台六個分區；參數的四層 Tier 模型、解析順序、漂移偵測、區網直連的運維筆記 | 已實作 |
| **C** | [C-F-瀏覽體驗.md](規格/C-F-瀏覽體驗.md) | 影集分組、相片庫排除封面、換頁捲動、iPhone 全螢幕 | 待做 |
| **G** | [G-已知缺陷.md](規格/G-已知缺陷.md) | 現在就壞掉的東西：G-1～G-6 與其他較小的，含每一條的根因 | 陸續修 |
| **H** | [H-相片檢視.md](規格/H-相片檢視.md) | PhotoSwipe 5、preview 衍生圖、EXIF 側欄。真正的瓶頸不是縮放 | 已實作 |
| **I** | [I-PDF文件庫.md](規格/I-PDF文件庫.md) | `document` 表、整檔快取、vendored PDF.js 閱讀器。最小版已實作，封面延後 | 最小版已實作 |
| **J** | [J-影片畫質.md](規格/J-影片畫質.md) | 遠端畫質的三層帳、**兩階 HLS 的規格**、四輪量測（v4／v5 有 VMAF）、ffmpeg 版本怎麼選、量測工具的兩個對齊雷、免費 GPU 的判斷、Cloudflare 與區網 TLS 怎麼設 | 量測已完成、兩階 HLS 待實作 |
| **K** | [K-字幕.md](規格/K-字幕.md) | 為什麼「預載 10 句」只解一半；開播背景預抽 ＋ 顯示與繁簡判斷解耦 | **已實作** |
| **L** | [L-目錄權限.md](規格/L-目錄權限.md) | 密碼登入沒有身分這件事；`folder_rule`／`folder_grant`、20 個端點與把關測試 | **已實作** |
| **N** | [N-刮削修正.md](規格/N-刮削修正.md) | 刮到錯的資料要怎麼修；`manual` 被完整重掃覆蓋的 bug；`guess_key` 會過期；類型認錯 | **已實作** |
| **O** | [O-掃描並行化.md](規格/O-掃描並行化.md) | 幾萬個檔案的片庫怎麼掃得快：先量再改（走訪走本機直讀 13 倍 > 並行化 2.7 倍）、相片並行、單一寫入者、為什麼並行度**不該**照 CPU 核心數開 | **需求書，待確認** |
| **P** | [P-瀏覽體驗改善.md](規格/P-瀏覽體驗改善.md) | 分頁可點頁碼（三組重複的分頁器併成一個）、相片瀑布流＋無限捲動、條目詳情的開關動畫；含一個無限捲動會踩到的排序並列 bug | **已實作** |
| **M** | [M-參數頁.md](規格/M-參數頁.md) | 66 列裡 19 列永遠不能改；實測 48 項裡 43 項被 .env 蓋住 → 狀態當第一軸；二層 sidenav、批次儲存、搜尋框 | **已實作** |

> **為什麼要分檔。** 原本是一個 94 KB、1636 行的單檔。單檔的好處是「一次搜尋全部命中」，
> 壞處是**它已經沒有人會從頭讀**：想查一件事要先滑過十章不相關的內容，
> 而每次改動都在同一個檔案上，diff 也看不出改的是哪個題目。
> 分檔之後 `rg` 一樣搜得到（範圍改成 `docs/`），而每一章都短到可以一次讀完。
> **索引頁刻意只放結論**：決策表與分期計畫是唯一需要常看的兩張表，
> 把它們留在最外層，就不必為了看「現在該做什麼」而打開任何一章。

---

## 分期計畫

> **v5.9 起改成「做完／在做／接下來」，不再用期別編號。**
> 原本的一～六期已經被實際順序追過去了（B 後台與 I PDF 都提前做完、H 插隊、
> J 的修正包在第一期就出手），繼續維護編號只會讓文件跟現實不一致 ——
> 而一份跟現實不一致的計畫，比沒有計畫更糟。

### 做完了

| 項目 | 備註 |
|---|---|
| **B ＋ B+　管理後台** | 六個分區都在（總覽／媒體庫／使用者／播放與轉碼／系統／紀錄），參數頁含 Tier 模型與漂移偵測 |
| **H　相片檢視** | PhotoSwipe 5 ＋ 1920px preview 衍生圖 ＋ EXIF 側欄 |
| **I　PDF 文件庫（最小版）** | 提前做得起來，是因為最小版**不產生任何磁碟衍生物**，所以不必等 A-4 的 `purge()` |
| **J 的修正包** | G-6、兩個 falsy-0、NVENC 參數分家、`bt2390` 無效值、`.env` 畫質值 |
| **J 的 ffmpeg 升級 ＋ VMAF 量測（v4／v5）** | 4.2.3 → BtbN **n8.1**（9.0 的 NVENC 要驅動 610，Pascal 拿不到）。量出 x264 與 NVENC 的分工、上限開到多少、CRF／cq 該設多少。過程中揪出量測工具自己的兩個對齊錯誤，第一批結果整批作廢重跑 —— 工具現在每輪都跑一次「對齊自檢」，而且兩條獨立的階梯在同碼率上量到同一個分數（差 0.1～0.4） |
| **J 的分段邊界表（第 0 層第 4 項）＋ 本機直讀對應表（第 −1 層）** | `media_keyframe` 存原始 keyframe 時間與位元組偏移（不存算好的邊界 —— 邊界規則還沒定案，而重掃一次是 42 分鐘）；`kf_state` 跟 `probe_state` 吃同一個失效訊號；掃描結束後才啟動的**單執行緒**背景佇列。`localfs.py` 的路徑防護九項全測。順帶問清楚一件事：`ffprobe` 的欄位名是 `pkt_pos` 不是 `pos`（後者偏移是空的），而偏移是 `BANDWIDTH` 峰值的唯一來源 |
| **A　資料層（A-1～A-4）** | 這一章其實一直是分散做掉的，只是索引沒更新。這次逐條對照後標成完成：儲存後端開關 · 批次寫入（條目走記憶體註冊表，不硬做成批次）· 時間欄位 · `purge()` 單一刪除進入點與孤兒清掃。**順手補完最後一個缺口**：時間點欄位有一半是浮點（`media_file.seen_at` 147 列、`photo.seen_at` 1490 列、`media_item` 48 列），寫入端統一成 `now_i()` 並做一次性取整；`duration`／`position` 沒有碰 —— 那是時間長度，取整會讓續播每次往前跳一秒 |
| **啟動診斷的兩個修正** | 日誌補上日期，並把 uvicorn 自己那幾行與 access log 也接管過來（`basicConfig` 管不到 uvicorn，所以那些行原本完全沒有時間戳）· 「還沒有管理員帳號」的提示分成兩種情況：**名單設了但沒人登入過**時不要再叫人去改 `.env`，而是講出真正的下一步（用那個 Gmail 登入一次）—— 一個把人導向錯誤方向的提示比沒有提示更糟 |
| **L 的授權清單修正** | 「升成管理員但授權沒有自動勾起來」—— 查下來是**畫面在說謊**：管理員看得到受限資料夾是靠角色（`can_read` 第一句就是 `is_admin`），不是靠授權列。**決定不在升級時自動寫入授權**：那會在降級時變成安靜的權限殘留，而為了修它改成降級自動刪，又會刪掉他升級前本來就有的授權。改的是畫面 —— 勾選清單只列一般帳號、管理員另起一行、把「勾選＝即使不是管理員也看得到」講明白。順帶修掉兩個坑：儲存是整份取代（會洗掉管理員身上的既有授權，前端要帶回去）、`role` 是 DB 字彙 `owner` 而畫面講「管理員」（端點改回 `is_admin` 布林）|
| **README** | 從零寫一份：快速開始、7 張 mermaid 圖（架構／播放決策／遠端播放時序／掃描時序／狀態機／設定優先序／登入流程）、資料庫與設定的說明、疑難排解對照表、規格書導覽 |
| **J 畫質標示** | 回報「畫質只有 804p」，查下來是標示問題不是畫質問題：1920×804 是 2.39:1 寬螢幕片的原始畫面高度（黑邊壓製時裁掉了），而片庫裡 1920 寬但高度不是 1080 的有 **88 部**、真正 1080 的只有 32 部 —— 照高度標的話那 88 部全部看起來像被降級。改成**依寬度判級別**（1920→1080p，同 Plex／Jellyfin），選單與片源資訊同時顯示級別與實際解析度 |
| **J 兩階 HLS 的第二批** | `build_remux_cmd()` 是獨立的一支而不是 `build_transcode_cmd()` 的分支（共用的不到一半，不共用的是「絕對不能出現」的那些；混在一起將來會有轉碼參數漏進 copy 路徑，而那不報錯、只讓「零損失」悄悄變成假的）· **零損失是驗過的**：測試真的產生一段上階並比對逐格 md5 · `-avoid_negative_ts make_zero` 讓非單調 DTS 的片源不被 mpegts muxer 拒收 · 已經是 AAC 但聲道數不符仍要轉（否則 `AUDIO_CHANNELS` 的降混在上階失效）· 一段失敗 → 整個檔案上階下線 ＋ 後台清單（**不做「退回 CPU 重試」**：copy 沒用到編碼器）· 遠端不走 direct（只在真的有上階時）· 開播插隊真的接上了（`request_soon()` 原本沒有任何呼叫點）· **第 −1 層接進五個 ffmpeg 呼叫點**（探測、轉碼、remux、縮圖、字幕、實測）|
| **J 兩階 HLS 的第一批** | profile key 加 `m0`／`m1` 階別維度（少了這一維，7.25 秒的 remux 分段與 6 秒的轉碼分段會落進同一個資料夾）· `rungs_for()` 是「有哪幾階」的唯一答案來源 · master 區網一階／遠端兩階、上階宣告**峰值** `BANDWIDTH` ＋ `AVERAGE-BANDWIDTH` · `TARGETDURATION` 照實際最長段填（上階 7.25 秒，填設定值 6 是規格違反）· `CODECS` 由新增的 `video_profile`／`video_level` 推、推不出來就整個省略 · `PROFILE_KEY_VERSION` 讓舊快取清一次。全部藏在 `HLS_TWO_RUNG=false` 後面 |
| **J 的量測結論套用（第 4.6 步）** | 值進 `.env`／`.env.example`（`X264_PRESET=medium`、`REMOTE_BITRATE_KBPS=8000`、`NVENC_CQ=25`、`TRANSCODE_CRF=17`、`FFMPEG_HWACCEL=nvenc`）；機制上把位元率控制抽成 `_rate_args()`：**有目標碼率時 NVENC 改成碼率導向**（`-b:v/-maxrate`，不給 `-cq`），x264 走 capped CRF，外層不再自己補 `-maxrate`（原本兩處都加，會出現同一個旗標兩次、後者靜默勝出）。順手抓到 `FFMPEG_HWACCEL` 的預設值兩邊不一致：參數頁與 `.env.example` 寫 `auto`、`config.py` 讀 `none` —— 乾淨安裝其實走 CPU，正好與「重編首選 NVENC」相反 |
| **J 的運維** | Cloudflare 端 Always Use HTTPS／HSTS、WARP 私有網路、區網直連的兩個關卡 |
| **K　字幕預抽** | 已快取 180 ms／未快取 5.2 秒（含 5 秒刻意暖機）出現第一句；順手修掉「只有簡體字幕的片只剩前 60 句」，並把文件的刪除接進 `purge()` |
| **N　刮削修正包** | 修掉「完整重掃蓋掉手動修正」；挖出 `guess_key` 會過期（改年份／類型／手動指定 TMDB 都會讓條目在下次掃描變成兩筆）；補類型切換、改片名／年份、重新分析、後台清單 |
| **L　目錄權限** | 兩張新表 ＋ `acl.py` 兩個 primitive ＋ 20 個端點 ＋ route table 掃描測試。實測：限制一個資料夾後，未授權的 viewer 從 117 個影片檔看到 81 個 |
| **M　參數頁改版** | 獨立分頁 ＋ 二層 nav ＋ 批次儲存 ＋ 搜尋。**規劃時的數字算錯了**：UI 上沒有「永遠唯讀」的列，實際是 48 項裡 **43 項被 `.env` 蓋住** —— 所以第一軸改成狀態而不是分類 |

### 現在在做

**（無）** —— K、M、L、N 都完成了。

### 接下來

| 順序 | 項目 | 為什麼排這裡 |
|---|---|---|
| 1 | **C／D／E／F　剩下的體驗修正** | 彼此獨立、風險低。C 做完要重新分組一次；F 需要在 iPhone 上實測 |
| 2 | **J 的三項量測** | libplacebo GPU tonemap（4K 那 6 部唯一的槓桿）· NVENC 的 spatial-AQ 異常 · `TRANSCODE_CRF` 在 medium preset 下複驗。三項都只影響「必須重編」的少數片源 |

### 之後

| 項目 | 前置條件 |
|---|---|
| **J　本機直讀的其餘受益項** | **ffmpeg 的讀取路徑全部接完了**（探測、轉碼、remux、縮圖、字幕、實測）。還沒接的是 `/api/stream/{id}` 改 `FileResponse`、掃描改 `os.scandir`、PDF 與相片衍生圖 —— 各自要動自己的模組，分開做才好回退 |
| **I　PDF 第一頁封面（pypdfium2）** | 要等 A-4 的 `purge()`，否則刪檔會留孤兒縮圖。同時要把「消失文件的刪除」從掃描迴圈搬進 `purge()` |
| **I　閱讀進度 `read_state`** | 形狀與 `play_state` 一致，跟首頁「繼續看」一起做 |
| **K 的 B 案　`-ss` 兩段式字幕** | 要先在實機量過 ffmpeg 的 webvtt 時間戳行為（`-copyts` 會不會歸零）。**沒量過不寫** |
| **區網真憑證（決策 17 的第三步）** | `lan.longhopick.com` ＋ DNS-01，把 `COOKIE_SECURE` 收回 `true` |

---

## 已確認的決策

| # | 決策 | 結論 |
|---|---|---|
| 1 | 時間欄位 | Python 端 epoch、SQLite 整數、MSSQL `DATETIME2`，加四個配套 |
| 2 | FTP 併發上限 | 量測工具 `app/ftpprobe.py` 已完成並驗證，由使用者在自己機器上執行 |
| 3 | 現有彈窗 | 保留簡化版，完整功能在後台 |
| 4 | G-5 縮圖 | 採徹底修法：檔名改內容雜湊 |
| 5 | 相片點圖放大 | 引入 PhotoSwipe 5（vendored），**並加 preview 衍生圖** —— 只加縮放不加衍生圖等於做出「可以放大但每次都要等」的燈箱 |
| 6 | PDF 的定位 | 第三種媒體類型（獨立 `document` 表）。**最小版已實作**。封面將來用 pypdfium2，**不用 PyMuPDF**，取捨點是 AGPL 授權 |
| 7 | PDF 讀取路徑 | 第一次開啟時整檔快取到本機再服務，**不讓 PDF.js 對 FTP 做 Range** —— 否則一份 PDF 就能把 FTP 連線池打爆。**已實作**（`DOC_CACHE`、自寫 206、LRU 2 GB）|
| 8 | 遠端畫質 | 解除 2.8 Mbps 的上限（上行 100 Mbps 撐得住）。**v4／v5 用 VMAF 定案**：`REMOTE_BITRATE_KBPS` **8000**（曲線轉折點，8000→12000 只有 +0.3～+0.4）、`TRANSCODE_CRF` **17**（暫定，preset 換 medium 後要再確認）、**`X264_PRESET` 改 `medium`**（同 CRF 下 +2.1 VMAF，只多花 9% 碼率）。v3 判「維持 veryfast」是用 SSIM，而 SSIM 在這個區間飽和，那 0.0012 是噪音。HDR 映射改 `hable`（`bt2390` 是無效值）。**已生效**（第 4.6 步：值進 `.env`／`.env.example`，機制上位元率控制抽成 `_rate_args()`） |
| 23 | 重編這條路的天花板 | **可以 remux 的那 97 部，重編到「看不出差異」比原檔還貴。**v5 實測片源 A（原檔 10.9 Mbps）：x264 要 CRF 15／**17.8 Mbps** 才到 VMAF 95，NVENC cq 22 花 9 Mbps 也只有 94.9。所以第 0 層的 Direct Stream 不是「比較好的選項」，是**唯一**能拿到透明畫質的選項。必須重編的那一類（4K→1080p）便宜得多：3.2 Mbps 就到 95 |
| 9 | 免費租用顯卡 | **不採用。**GPU 換的是速度不是畫質；Colab 之類的條款明文禁止 media serving；而且片源在自家 FTP，雲端 GPU 得先上傳整個片庫。本機的 GTX 1070（Pascal）可用於併發，畫質優先仍走 x264 |
| 12 | 硬體解碼 | **已修正**：不要單獨開 `-hwaccel cuda`。實測混合模式更慢（1080p 11.6x→9.7x、4K 0.44x→0.34x），每幀要在 GPU 與 CPU 之間搬兩次。要整條鏈留在 GPU（`-hwaccel_output_format cuda` ＋ `scale_cuda` ＋ GPU tonemap）。**v4 補充**：全 GPU 鏈在 1080p 只從 8.92x 到 9.08x（雜訊），而它只在不需要 tonemap 時適用 —— 那種片源本來就該 remux。真正的槓桿是 4K 的 **libplacebo GPU tonemap**（8.1 已經帶了，**還沒量**）：目前 4K 的 x264 1.15x／NVENC 1.37x 都低於 `PREFETCH_MIN_SPEED=1.5`，預轉不會啟動 |
| 15 | NVENC 的品質參數 | **與 x264 分開。**實測 `-cq 21` 讓 NVENC 吐出 10.2 Mbps（原檔 10.9），幾乎沒壓縮。`TRANSCODE_CRF` 一個數字不能同時服務兩種編碼器。**v5 定案 `NVENC_CQ=25`**（4K→1080p 到 VMAF 95.6／3,185 kbps，1080p 93.9／5,613 kbps，兩邊都在 8000 上限之內）。**v4 附帶**：NVENC 應該走碼率導向而不是 cq 導向 —— cq 28 下「調滿」花 3,305 kbps 拿到 92.8、「現況」花 2,579 kbps 拿到 94.0，更多碼率更低分，最可能是 `-spatial-aq`（待查）。綁碼率的階梯不受這個影響。**已生效**：有目標碼率時 NVENC 走 `-b:v/-maxrate` 而不是 `-cq` —— 同時也是兩階 HLS 下階能宣告 `BANDWIDTH` 的前提（cq 導向給不出上界） |
| 14 | 本機直讀 | 加 `LIBRARY_LOCAL_ROOTS`（Tier 4）把 FTP 路徑對應到本機路徑。片庫本來就在 `D:\1.FTP`，現在每段轉碼要經過「HTTP → FastAPI → FTP → 磁碟」四層。FTP 保留當備援。**對應表與路徑防護已實作**（`localfs.py`，九項防護全測；每一種拒絕都回 `None` 走 FTP，失效模式是「變慢」不是「播不出來」）。**順序上的修正**：它不是最佳化，是第 0 層的前置條件 —— 邊界表 6.2 秒／GB 是用本機路徑量的，走四層的話那個數字不成立 |
| 13 | G-6 的解析 bug | 一行 regex（數值欄改成可有可無）。修好之前，走硬體編碼時 `TRANSCODE_CRF` 對畫質沒有作用 |
| 11 | ffmpeg 版本 | **已完成。**4.2.3（ImageMagick 附帶）→ BtbN **`ffmpeg-n8.1-latest-win64-gpl`**，`.env` 用完整路徑指定 `FFMPEG_PATH`／`FFPROBE_PATH`（不加引號）。**刻意不用 9.0**：9.0／master 是拿 nv-codec-headers SDK 13.1 編的、要驅動 610，而 R580 是 NVIDIA 給 Pascal 的最後一個分支 —— GTX 1070 永遠拿不到 610。BtbN 每個分支釘不同標頭，8.0／8.1 用 SDK 13.0（驅動 570 以上），而 libvmaf、libsvtav1、`p1`~`p7`、`-tune hq`、`-multipass` 一個都沒少 |
| 10 | 影片流量路徑 | 移出 Cloudflare 橘雲代理，改走 **WARP private network**（Zero Trust 不是 CDN，不受該條款限制）；控制介面留在 Tunnel。**引用修正**：Section 2.8 已於 2023 年退場，限制現在寫在 CDN 的 Service-Specific Terms，實質未變 |
| 16 | PDF.js 的 build | **必須用 legacy build。**預設 build 呼叫 `Map.prototype.getOrInsertComputed`，Chromium 141 與 Safari 都沒有，實測整個閱讀器打不開（`TypeError`）。cmaps 刻意不帶（169 檔），真的遇到無內嵌 CJK 字型的 PDF 再補 |
| 17 | 區網登入的 TLS | 先走「IP 直連 ＋ `COOKIE_SECURE=false` ＋ Cloudflare 端 Always Use HTTPS/HSTS」；長期改成 `lan.longhopick.com` ＋ **DNS-01** 憑證，把 `COOKIE_SECURE` 收回 `true`。**HSTS 的 `includeSubDomains` 不能先開** —— 它會把區網那個 plain http 的子網域一起鎖成 https |
| 18 | 字幕預先載入 | 「預載 10 句」只對「從頭播」有效；跳到中間時 ffmpeg 從 0 秒按序吐，需要的句子最後才到。**決定做「開播背景預抽進快取」**，並把顯示與繁簡判斷解耦（先畫、60 句時才決定要不要換軌）。`-ss` 兩段式列為選配，**量過才寫** |
| 19 | 受限目錄的身分 | **只綁 Google 帳號（`app_user`）。**密碼登入的 token 沒有 `uid`，沒有身分可以判斷就不能給。密碼 viewer 一律看不到受限目錄；admin 看得到全部。不靠 Google 的具名帳號是另一件事 |
| 20 | 受限目錄的預設 | **「沒被標記的資料夾所有人可見」**，而不是每個帳號一張白名單 —— 白名單的維護成本會落在「每次新增資料夾都要改每個帳號」上，漏一個就是他看不到新片。回 **404 不回 403** |
| 24 | 遠端該給幾階 | **區網一階（remux），遠端兩階，由客戶端的緩衝水位選 —— 伺服器不猜頻寬。**理由有三：remux 的畫質免費但**頻寬不免費**（美國隊長 remux 要 19.9 Mbps，遠端吃不下）；單一階讓客戶端 ABR 完全沒有作用空間；而「伺服器估頻寬選碼率」是 Netflix 在 50 萬使用者上驗證後**放棄**的做法（實際／估計容量比值低到 0.03，改用緩衝水位後重新緩衝率降 10～20%）。**前置量測改了兩處**：(1) 上階要宣告**峰值** 21,703 kbps（平均 10,899、1.99 倍），而觀看端約 4 Mbps —— **上階遠端實際選不到**，兩階真正的價值是「區網無損零轉碼 ＋ 遠端一階有界的轉碼」；(2) 因此**兩階不共用邊界**（上階照 keyframe 7.34 秒、下階維持短段），用罕見切換的一次停頓換常態路徑的開播速度。下階目標約 3 Mbps。邊界表 6.2 秒／GB、全庫 42 分鐘 → 只能在掃描時走背景佇列算 |
| 22 | 量測要怎麼才可信 | **每一輪都要有一個「已知答案」的檢查。**v4 的第一批結果整批是錯的：參考與變體錯開一格，分數不會爆掉、只會一路偏低而且對位元率沒反應。抓到它靠的是「把片段無損重編，分數只能是 100」—— 量到 17.2。工具現在固定把這一列排在最前面，沒過就作廢。同一個道理已經在 FTP 那支工具上付過學費（假的 0.75 MB/s） |
| 25 | 邊界表存什麼 | **存原始的 keyframe 時間與位元組偏移，不存算好的邊界。**邊界規則還沒定案（「湊滿 ≥ 分段長度」把 6 秒的設定變成 7.34 秒，另一種是取「離目標最近」），而重掃一次是 42 分鐘 —— 存原始資料，換規則只要重跑 `derive_bounds()`。配套三項：`kf_state` 與 `probe_state` 吃**同一個**失效訊號（同一句 `ON CONFLICT`，檔案變動的判斷只該有一份）；`table_for()` 只在 `ok` 時回表（`pending` 的表可能是舊檔案的，照著切會切出對不上的段）；`skipped` 一定要寫原因，留空白會被誤判成「還沒跑到」 |
| 26 | A-5 MSSQL 媒體庫 | **不做。**媒體庫在 SQLite 上是單一寫入者、無競態，批次化後重掃只用個位數交易；MSSQL 目前只服務帳號那一半而且已經在跑。搬過去換到的是「多台機器共用媒體庫」，**而現在只有一台**，代價卻是多一個必須在線的外部相依、每次寫入變成跨行程往返、備份從「複製一個檔案」變成兩套流程。**前提出現（真的有第二台機器）再說**，A 章的實作提示保留 |
| 21 | 參數頁版面 | 獨立分頁 ＋ sidenav 二層（後台左邊已經有一排，不要變兩排）；19 項唯讀收進最後一個分類；**批次儲存**（所有 `lockout` 參數都是 `ENV_ONLY`、不在 UI，所以批次沒有把自己鎖死的風險）；加搜尋框 |

### 決策 2 的補充

`FTP_HOST` 是 `127.0.0.1`，FTP 與服務跑在同一台機器，開發環境連不到，
也沒有能在該機器執行指令的工具。因此改為交付量測工具。

**v1 的測速是錯的，已修正。** 第一次交付的版本在使用者的伺服器上量到
「每條連線都剛好 0.75 MB/s、併發完美線性」。回頭比對發現開發環境的 loopback
測試也是同一個數字 —— 兩台完全不同的伺服器不可能一致，那是工具本身的產物。

原因：測速迴圈抓滿 3MB 後呼叫 `ftplib.abort()` 中止傳輸，而 **`abort()` 會讓
控制連線失去同步** —— 下一個指令會讀到上一個指令殘留的回應（實測：PASV 讀到
`200 Type set to: Binary`），整條連線從此壞掉。所以每條連線只成功跑完**一輪**，
剩下的時間全在空轉。而「3 MB ÷ 4 秒時間窗」＝ 0.75 MB/s，
在任何伺服器上都會量到同一個數字，看起來像真的資料。

修正：改用 `ntransfercmd()` 拿原始 socket，讀夠就直接關掉資料連線
（伺服器回 426，預期中），控制連線保持乾淨。實測對照：

| 做法 | 4 秒內完成輪數 | 量到的速度 | 控制連線 |
|---|---|---|---|
| v1：`retrbinary` + `abort()` | 1 | 假數字 | **壞掉** |
| 整檔下載不中斷 | — | 1763 MB/s（真正天花板） | 正常 |
| v2：`ntransfercmd` + 提前關閉 | 305 | 229 MB/s | 正常 |

同時加上**輪數回報**：某一級如果只跑完一兩輪，那個 MB/s 就是「一輪除以時間窗」
而不是頻寬，會標示為不可信並排除在推算之外。這種錯不該再靠人眼發現。

**記憶體約束也修了。** 原本用 `MAX_PHOTO_MB`（80MB）估每條連線的佔用，
但那是「超過就不讀」的門檻，不是實際佔用量。改用資料庫裡的真實最大值 ——
實際最大的一張是 30.9MB（p95 19.3MB、中位數 2.02MB），保守程度差了 2.6 倍。
還沒掃過相片時才退回用門檻估，並註明「掃過之後再跑一次會更準」。

**連線數上限的部分準確度已驗證** — 架了兩台上限已知的 FTP 伺服器測試：

| 伺服器設定 | 工具量到 | 結果 |
|---|---|---|
| `max_cons = 7` | 6 條成功，第 7 條被拒 | 正確 |
| `max_cons = 15` | 14 條成功，第 15 條被拒 | 正確 |

執行方式：`測試FTP併發.bat`。只做登入、列目錄、下載一小段測速，不寫入任何東西。
這支工具之後會變成後台「播放與轉碼」分區的一顆按鈕。

### 量測結果推翻了「多執行緒該加在讀相片」這個前提

修好之後在使用者的伺服器上實測：**單條 295 MB/s、3 條 1045 MB/s，
679 張共 2.7GB 只要約 10 秒讀完**。FTP 完全不是瓶頸。

於是進一步量相片階段的時間到底花在哪（用符合真實大小分布的圖片跑 `app/photo.py`）：

| 階段 | 679 張的成本 | 佔比 |
|---|---|---|
| FTP 讀取 | ~10 秒 | 6% |
| 讀 EXIF 與尺寸 | 0.5 秒 | 0.3% |
| **產生縮圖** | **~149 秒** | **94%** |

`read_info` 幾乎免費 —— PIL 的 `Image.open` 是惰性的，只讀檔頭。
**所有成本都在 `make_thumb` 的完整解碼。**

**因此第二期的做法要改，而且有一個比多執行緒更好的解法：**

1. **`im.draft("RGB", (480, 480))`** —— 讓 JPEG 解碼器直接以 1/2、1/4、1/8 尺寸解碼，
   不必先把整張全解出來再縮小。實測 **219.6 ms → 86.2 ms，快 2.5 倍**
   （679 張：149 秒 → 59 秒）。畫質差異平均像素差 1.84/255，視覺上看不出來。
   **一行程式碼，不增加記憶體，沒有執行緒的複雜度。優先做這個。**
2. 多執行緒再疊上去。PIL 的解碼會釋放 GIL，實測 2 核心上約 1.8～2.0 倍。
3. 兩個都做：149 秒 → 約 25～30 秒。

**`PHOTO_CONCURRENCY` 的預設值要照 CPU 核心數算，不是照 FTP 上限算。**
量測工具已更新：頻寬轉折點只作為資訊回報，不列入限制條件 ——
拿一個「跑滿頻寬時」的門檻去限制一個「只佔 6% 時間」的工作是錯的。
